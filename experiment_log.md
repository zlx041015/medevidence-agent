# MedEvidence Agent 实验与迭代日志

这份日志按项目演进顺序记录关键阶段、修改内容、观察结果和设计洞察。它既可以作为项目开发记录，也可以作为后续面试讲述时的素材来源。

## 实验 1：规则版工作流原型

### 目标

从 0 到 1 搭建一个可运行的医学证据工作流骨架。

### 主要修改

- 建立 `Planner -> Retriever -> Extractor -> Verifier -> Writer` 基础链路
- 使用本地 `mock_sources.json` 作为来源

### 观察

- 系统已经可以完成单个医学问题输入、来源筛选、规则抽取和结果输出
- 整体结构清晰，但仍然依赖手工规则和 mock 数据

### 洞察

先把工作流搭起来，再逐步引入 LLM 和真实来源，比一开始就追求复杂能力更稳妥。

## 实验 2：参数调优

### 目标

验证 `confidence_threshold`、`evidence_score_threshold` 和 `top_k` 对结果的影响。

### 主要修改

- 分别调高/调低审核阈值、来源过滤阈值和候选来源数量

### 观察

- 审核阈值决定系统是否放行，而不改变证据本身
- 过滤阈值过高会漏信息，过低会引入噪声
- `top_k` 太小会导致证据单薄，太大又会引入无关来源

### 洞察

证据系统优化的核心不是单点参数，而是召回率、精确率和审核严格度之间的平衡。

## 实验 3：规则版 Extractor 与 Verifier 升级

### 目标

让系统不再只输出固定模板，而是开始体现来源内容差异。

### 主要修改

- Extractor 从固定 `claim` 升级为基于来源内容的规则抽取
- Verifier 开始根据 `claim` 内容而不是纯数量做判断

### 观察

- 不同来源开始生成不同的 `claim`
- 中间证据对象的结构化程度提高

### 洞察

即使还是规则系统，只要中间表示更结构化，效果也会明显提升。

## 实验 4：Planner 规则泛化

### 目标

让规则版 Planner 不只支持少数主题，而是覆盖更一般的医学问题表达。

### 主要修改

- 为 Planner 增加更通用的关键词扩展逻辑
- 覆盖治疗、筛查、并发症、风险管理等常见问题模式

### 观察

- 当 LLM Planner 失败时，规则 fallback 不再明显跑偏

### 洞察

Agent 系统中的 fallback 必须与主任务保持方向一致，否则下游会整体偏离。

## 实验 5：首次引入 LLM

### 目标

把系统从纯规则版本推进到 LLM 驱动版本。

### 主要修改

- 新增统一 `llm.py`
- 将 `Planner`、`Extractor`、`Writer` 改为优先调用 LLM，失败时 fallback 到规则版

### 观察

- 需要处理第三方兼容接口的 `base_url` 与返回格式问题
- LLM 节点接入后，结果更自然，表达更接近真实助手

### 洞察

统一 LLM 调用层 + 每个节点 fallback，是构建稳健 Agent 工作流的关键。

## 实验 6：接入真实来源 PubMed

### 目标

用真实来源替代纯 mock 数据。

### 主要修改

- 接入 PubMed `esearch`
- 接入 PubMed `efetch`
- 将真实文献映射到 `SourceDocument`

### 观察

- 系统已经可以从真实文献构建输入，不再完全依赖本地示例

### 洞察

一旦接入真实来源，问题重心就从“流程能不能跑”转为“检索质量够不够好”。

## 实验 7：引入轻量 RAG

### 目标

让系统从“整段摘要喂给模型”升级为更接近 RAG 的流程。

### 主要修改

- 新增 chunk 切分
- 新增 chunk 级检索
- 增加 sparse + dense 的轻量混合检索思路

### 观察

- 系统开始围绕更相关的局部片段组织证据，而不是整段全文

### 洞察

即使不引入重型向量数据库，轻量 RAG 也能显著提升结构完整度。

## 实验 8：来源质量控制

### 目标

让系统不仅“有来源”，还要“区分来源质量”。

### 主要修改

- 对来源类型进行区分
- 加入 `quality_score`
- 在 verifier 中考虑来源等级

### 观察

- 系统不再把所有文献视作同一质量

### 洞察

医学证据系统的可信度，不仅取决于有没有文献，也取决于文献等级和时效性。

## 实验 9：GUI 产品化展示

### 目标

提升项目的可展示性和交互性。

### 主要修改

- 新增桌面 GUI
- 支持进度展示、历史记录、结果复制导出和来源打开

### 观察

- 项目从命令行原型升级为更适合演示的工具

### 洞察

GUI 不是算法核心，但对面试展示和项目完成度有明显帮助。

## 实验 10：加入 benchmark、baseline 与消融

### 目标

让项目从“能跑通”升级为“可评测”。

### 主要修改

- 新增 benchmark 数据结构
- 新增 baseline 方法
- 新增自动指标
- 新增消融实验
- 新增结构化结果导出

### 观察

- 项目开始具备“能比较、能复盘、能说明为什么更好”的能力

### 洞察

有亮点的 AI 项目，不只是功能闭环，还要有 benchmark、对比实验和失败案例分析。

## 实验 11：多病种 benchmark 扩展

### 目标

把项目从少数专项病种示例升级成通用病症问题系统。

### 主要修改

- benchmark 扩展到 100 条问题
- 覆盖 30 多种常见病症
- 按治疗、筛查、随访、风险管理四类问题组织

### 观察

- benchmark 不再是局部专题问题集，而更像通用病症样本集

### 洞察

对于求职和项目展示来说，广谱覆盖往往比少数专题样例更能体现系统能力。

## 实验 12：多病种来源扩展

### 目标

让 benchmark 的广度与来源数据广度保持基本一致。

### 主要修改

- 扩展 `guideline_sources.json`
- 扩展 `mock_sources.json`
- 增加心血管、呼吸、感染、消化、风湿、泌尿、精神心理和皮肤病等方向来源

### 观察

- 系统不再只适配少数病种的离线路径

### 洞察

如果 benchmark 变广但来源不变广，项目就会变成“看起来更大，实际上接不住”。

## 实验 13：通用病症规则泛化

### 目标

让系统在 rule-based 路径里也具备通用病症识别能力。

### 主要修改

- 扩展 `planner.py` 的病种关键词规则
- 扩展 `verifier.py` 的主题关键词识别

### 观察

- fallback 路径不再只偏向糖尿病或甲状腺

### 洞察

要做通用系统，不能只改数据集，也必须同步扩规则层。

## 实验 14：文档与叙事统一

### 目标

把项目对外叙事统一成“通用常见病症系统”。

### 主要修改

- 重写 README
- 重写简历稿、面试稿和架构说明
- 增加 benchmark 指南

### 观察

- 项目从代码到文档口径更一致

### 洞察

对于求职项目来说，文档叙事本身就是系统设计的一部分。

## 当前项目定位总结

当前版本可以定义为：

`一个面向通用常见病症问题的证据驱动型医学 Agent + 轻量 RAG + benchmark 评测原型系统`

### 当前优势

- 多阶段工作流
- 多病种 benchmark
- 多病种来源数据
- LLM 节点与规则 fallback
- 轻量 RAG
- verifier 可解释检查
- baseline 与消融实验
- GUI 展示

### 当前仍有缺口

- 还不是重型向量数据库平台
- 还缺真实人工盲评闭环
- 还缺更大规模真实来源知识库
- 还缺更强的 reranker 和 judge model 评测

### 最终结论

当前版本已经足够作为“可讲述、可演示、可评测”的医疗 AI 应用项目，用于简历和面试都具备较强支撑力。
